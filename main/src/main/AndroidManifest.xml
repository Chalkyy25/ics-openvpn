<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- NOTE: Remove if you don't actually read external files on Android 13+ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33"/>

    <!-- If you truly need to list all apps for an "Allowed Apps" picker, keep this.
         Prefer <queries> package visibility if possible for Play compliance. -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"/>

    <application
        android:name=".core.ICSOpenVPNApplication"
        android:allowBackup="true"
        android:appCategory="productivity"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/blinkt"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="UnusedAttribute">

        <!-- Brand + API base URL for RetrofitClient -->
        <meta-data android:name="AIOVPN_APP_NAME" android:value="AIO VPN"/>
        <meta-data android:name="AIOVPN_API_BASE_URL" android:value="https://aiovpn.co.uk/api"/>

        <meta-data
            android:name="android.content.APP_RESTRICTIONS"
            android:resource="@xml/app_restrictions"/>

        <!-- Launcher -->
        <activity
            android:name="de.blinkt.openvpn.ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Main (post-login) -->
        <activity
            android:name="de.blinkt.openvpn.ui.MainActivity"
            android:exported="false"/>

        <!-- Server picker -->
        <activity
            android:name="de.blinkt.openvpn.ui.ServerPickerActivity"
            android:exported="false"/>

        <!-- In-app OpenVPN log window (for diagnostics) -->
        <activity
            android:name="de.blinkt.openvpn.activities.LogWindow"
            android:label="VPN Logs"
            android:exported="false"/>

        <!-- LaunchVPN shim dialog (ICS) -->
        <activity
            android:name=".LaunchVPN"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:taskAffinity=".LaunchVPN"
            android:theme="@style/blinkt.dialog"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Core VPN service (must NOT be exported) -->
        <service
            android:name=".core.OpenVPNService"
            android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:process=":openvpn">
            <intent-filter>
                <action android:name="android.net.VpnService"/>
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn"/>
        </service>

        <!-- Optional public API service; set exported=false if you don't expose API -->
        <service
            android:name=".api.ExternalOpenVPNService"
            android:exported="true"
            android:process=":openvpn"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="de.blinkt.openvpn.api.IOpenVPNAPIService"/>
            </intent-filter>
        </service>

        <service
            android:name=".core.OpenVPNStatusService"
            android:exported="false"
            android:process=":openvpn"/>

        <!-- Boot + lifecycle -->
        <receiver
            android:name=".OnBootReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <service
            android:name=".core.keepVPNAlive"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:process=":openvpn"/>

        <!-- Remote action/dialogs (unchanged) -->
        <activity
            android:name=".api.RemoteAction"
            android:launchMode="singleInstance"
            android:autoRemoveFromRecents="true"
            android:excludeFromRecents="true"
            android:taskAffinity=".LaunchVPN"
            android:theme="@style/blinkt.dialog"
            android:noHistory="true"
            android:exported="false"/>

        <activity
            android:name=".api.GrantPermissionsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".api.ConfirmDialog"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <!-- Quick action aliases -->
        <activity-alias
            android:name=".api.ConnectVPN"
            android:exported="true"
            android:targetActivity=".api.RemoteAction"/>
        <activity-alias
            android:name=".api.DisconnectVPN"
            android:exported="true"
            android:targetActivity=".api.RemoteAction"/>
        <activity-alias
            android:name=".api.PauseVPN"
            android:exported="true"
            android:targetActivity=".api.RemoteAction"/>
        <activity-alias
            android:name=".api.ResumeVPN"
            android:exported="true"
            android:targetActivity=".api.RemoteAction"/>

        <!-- Optional FileProvider (uncomment if you share files/ovpn) -->
        <!--
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
        -->
    </application>
</manifest>